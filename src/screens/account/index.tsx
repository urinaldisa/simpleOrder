import React from 'react';
import {StyleSheet, View} from 'react-native';
import {Text} from '../../components/atom';

const Account = () => {
  return (
    <View>
      <Text type="bold" color="yellow">
        Account Page
      </Text>
    </View>
  );
};

export default Account;

const styles = StyleSheet.create({});
