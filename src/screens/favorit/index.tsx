import React from 'react';
import {StyleSheet, View} from 'react-native';
import {Text} from '../../components/atom';

const Favorit = () => {
  return (
    <View>
      <Text type="semibold">Favorit Screen</Text>
    </View>
  );
};

export default Favorit;

const styles = StyleSheet.create({});
