import React from 'react';
import {StyleSheet, View} from 'react-native';
import {Button, Text, TextInput} from '../../components/atom';

const Home = () => {
  return (
    <View>
      <Text type="regular">React Native FE</Text>
    </View>
  );
};

export default Home;

const styles = StyleSheet.create({});
